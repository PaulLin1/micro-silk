.DS_Store
.env
.env*.local
/node_modules/

# Next.js
/.next/
/out/
/build/
next-env.d.ts
*.tsbuildinfo

# Vercel
.vercel

# Raw arena data dumps — reproducible via scripts/ingest_arena.ts,
# and the real copy lives in Postgres once loaded.
/blocks.csv
/channels.csv
/connections.csv
/users.csv

# Large embedding CSV produced by ml/embed_to_csv.py — regenerate on the
# GPU box, never commit; the real copy lives in Postgres once loaded.
/ml/embeddings.csv
